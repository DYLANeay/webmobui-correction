Fetch and merge the latest changes from the upstream (parent) repository into the current branch.

Run `git fetch upstream` then `git merge upstream/main`. Show me a summary of new commits and changed files. If there are merge conflicts, list them so I can resolve them.